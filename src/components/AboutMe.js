import React from "react";

// Whenever we try to render an array containing JSX, React knows to render each JSX element separately
function AboutMe() {
  return (
   <div>

    <h2>This is where the about me info goes</h2>

   </div>
  );
}

export default AboutMe;